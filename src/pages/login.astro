---
export const prerender = true;

import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="TechMigos Secure Portal Login"
  description="Secure TechMigos portal login for clients and company team members to access projects, support, invoices, finance, and CRM workflows."
  noIndex={true}
>
  <section class="min-h-screen bg-slate-100 px-4 py-10 sm:py-16">
    <div class="mx-auto grid max-w-5xl overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_24px_70px_rgba(15,23,42,0.12)] lg:grid-cols-[0.92fr_1.08fr]">
      <aside class="bg-slate-950 p-7 text-white sm:p-10">
        <a href="/" class="inline-flex items-center gap-3" aria-label="TechMigos home">
          <img src="/icon.png" alt="" width="42" height="42" class="h-11 w-11 rounded-lg bg-white/10 object-contain p-1" />
          <span class="text-lg font-black tracking-tight">TechMigos</span>
        </a>
        <div class="mt-16 max-w-sm">
          <p class="text-xs font-black uppercase tracking-[0.18em] text-emerald-300">Company portal</p>
          <h1 class="mt-4 text-4xl font-black leading-tight">Work starts with the right access.</h1>
          <p class="mt-5 text-sm leading-7 text-slate-300">Use your assigned username or work email. Your access is checked before opening your workspace.</p>
        </div>
        <div class="mt-12 border-t border-white/10 pt-5 text-xs leading-6 text-slate-400">Need access or a password reset? Contact your TechMigos administrator.</div>
      </aside>

      <div class="p-6 text-slate-950 sm:p-10">
        <div class="mx-auto max-w-md">
          <p class="text-xs font-black uppercase tracking-[0.16em] text-emerald-700">Secure sign in</p>
          <h2 class="mt-3 text-3xl font-black tracking-tight">Welcome back</h2>
          <p class="mt-2 text-sm leading-6 text-slate-500">Enter your account details to continue.</p>

                <form id="portal-login-form" class="mt-8 space-y-5">
                  <label class="block" for="login-id">
                    <span class="form-label">Username or email</span>
                    <input
                      id="login-id"
                      name="login_id"
                      type="text"
                      required
                      class="form-input form-control form-control--muted focus-visible:!border-emerald-500 focus-visible:!ring-emerald-500/20"
                      autocomplete="username"
                      inputmode="email"
                      autocapitalize="none"
                      spellcheck="false"
                      placeholder="username or work email"
                    />
                  </label>

                  <div class="flex justify-end"><a href="/reset-password" class="text-sm font-bold text-emerald-700 hover:text-emerald-900">Forgot password?</a></div>

                  <label class="block" for="password">
                    <span class="form-label">Password</span>
                    <span class="relative block">
                      <input
                        id="password"
                        name="password"
                        type="password"
                        required
                        class="form-input form-control form-control--muted pr-16 focus-visible:!border-emerald-500 focus-visible:!ring-emerald-500/20"
                        autocomplete="current-password"
                        placeholder="Enter your password"
                      />
                      <button
                        id="password-toggle"
                        type="button"
                        class="absolute right-1 top-1/2 grid h-11 w-11 -translate-y-1/2 place-items-center rounded-[8px] text-slate-500 transition-colors hover:bg-slate-200 hover:text-slate-900 focus-visible:ring-emerald-500"
                        aria-label="Show password"
                        aria-controls="password"
                      >
                        <svg id="password-toggle-icon" aria-hidden="true" viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                          <path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"></path>
                          <circle cx="12" cy="12" r="3"></circle>
                        </svg>
                      </button>
                    </span>
                  </label>

                  <button
                    id="login-button"
                    type="submit"
                    class="inline-flex min-h-[52px] w-full items-center justify-center gap-3 rounded-[8px] bg-emerald-400 px-6 py-3 text-base font-black text-slate-950 shadow-[0_14px_30px_rgba(52,211,153,0.28)] transition-[background-color,box-shadow,transform] duration-200 hover:-translate-y-0.5 hover:bg-emerald-300 focus-visible:ring-emerald-500 disabled:translate-y-0 disabled:cursor-not-allowed disabled:bg-slate-300 disabled:text-slate-500 disabled:shadow-none"
                    aria-describedby="login-status"
                  >
                    <span id="login-button-text">Sign in</span>
                    <span id="login-spinner" class="hidden h-4 w-4 rounded-full border-2 border-slate-950/30 border-t-slate-950 animate-spin" aria-hidden="true"></span>
                  </button>

                  <p id="login-status" class="min-h-[1.25rem] text-sm text-slate-500" role="status" aria-live="polite"></p>
                </form>

                <p class="mt-6 border-t border-slate-200 pt-5 text-xs leading-5 text-slate-500">This is a private TechMigos workspace. Public sign-up is not available.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
  </section>

  <script is:inline>
    const form = document.getElementById('portal-login-form');
    const statusEl = document.getElementById('login-status');
    const button = document.getElementById('login-button');
    const buttonText = document.getElementById('login-button-text');
    const spinner = document.getElementById('login-spinner');
    const passwordInput = document.getElementById('password');
    const passwordToggle = document.getElementById('password-toggle');
    const passwordToggleIcon = document.getElementById('password-toggle-icon');

    function setStatus(message, isError) {
      if (!statusEl) return;
      statusEl.textContent = message || '';
      statusEl.className = `min-h-[1.25rem] text-sm ${isError ? 'text-rose-600' : 'text-slate-500'}`;
      statusEl.setAttribute('role', isError ? 'alert' : 'status');
    }

    function setLoading(isLoading, label) {
      if (!button) return;
      button.disabled = isLoading;
      button.setAttribute('aria-busy', isLoading ? 'true' : 'false');
      if (buttonText) buttonText.textContent = label || 'Sign in';
      spinner?.classList.toggle('hidden', !isLoading);
    }

    passwordToggle?.addEventListener('click', () => {
      if (!passwordInput || !passwordToggleIcon) return;
      const isVisible = passwordInput.getAttribute('type') === 'text';
      passwordInput.setAttribute('type', isVisible ? 'password' : 'text');
      passwordToggle.setAttribute('aria-label', isVisible ? 'Show password' : 'Hide password');
      passwordToggleIcon.innerHTML = isVisible
        ? '<path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"></path><circle cx="12" cy="12" r="3"></circle>'
        : '<path d="M17.94 17.94A10.94 10.94 0 0 1 12 19C5.5 19 2 12 2 12a18.45 18.45 0 0 1 5.06-5.94"></path><path d="M9.9 4.24A10.77 10.77 0 0 1 12 4c6.5 0 10 8 10 8a18.5 18.5 0 0 1-2.16 3.19"></path><path d="M14.12 14.12a3 3 0 0 1-4.24-4.24"></path><path d="M1 1l22 22"></path>';
      passwordInput.focus();
    });

    form?.addEventListener('submit', async (event) => {
      event.preventDefault();
      setLoading(true, 'Checking access...');
      setStatus('Checking your portal access...', false);

      try {
        if (!window.tmSupabase) throw new Error('Auth client not ready. Please refresh the page.');
        const data = Object.fromEntries(new FormData(form).entries());
        const loginId = String(data.login_id || '').trim();
        const password = String(data.password || '');
        let email = loginId;

        if (!loginId.includes('@')) {
          const { data: resolvedEmail, error: rpcError } = await window.tmSupabase
            .rpc('get_email_by_username', { p_username: loginId });
          if (rpcError || !resolvedEmail) {
            throw new Error('Username not found. Please use your email address.');
          }
          email = resolvedEmail;
        }

        const { data: authData, error: authError } = await window.tmSupabase.auth.signInWithPassword({
          email,
          password,
        });

        if (authError) {
          throw new Error(authError.message === 'Invalid login credentials'
            ? 'Email or password is incorrect.'
            : authError.message);
        }

        const { data: profile, error: profileError } = await window.tmSupabase
          .from('crm_profiles')
          .select('role, name, status, must_change_password')
          .eq('auth_user_id', authData.user.id)
          .limit(1)
          .single();

        if (profileError || !profile) throw new Error('Portal access is not provisioned for this account.');
        if (profile.status !== 'active') throw new Error('This account is not active yet. Please contact your TechMigos administrator.');

        const { error: lastLoginError } = await window.tmSupabase.rpc('record_crm_last_login');
        if (lastLoginError) console.warn('Could not record last login:', lastLoginError.message);

        setStatus('Opening your workspace...', false);
        if (buttonText) buttonText.textContent = 'Opening workspace...';
        location.href = profile.must_change_password ? '/change-password' : (profile.role === 'client' ? '/client' : '/company');
      } catch (error) {
        setStatus(error instanceof Error ? error.message : 'Could not sign in.', true);
        setLoading(false);
      }
    });
  </script>
</BaseLayout>
