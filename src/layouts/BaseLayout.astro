---
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  noIndex?: boolean;
  hideNavbar?: boolean;
}

const {
  title,
  description = 'TechMigos - Building Tomorrow, Together. We partner with startups and enterprises to design, build, and ship world-class digital products.',
  ogImage = '/og-image.png',
  ogType = 'website',
  noIndex = false,
  hideNavbar = false
} = Astro.props;

const siteOrigin = Astro.site?.toString().replace(/\/$/, '') || 'https://www.techmigos.com';
const canonicalURL = new URL(Astro.url.pathname, siteOrigin);
const absoluteOgImage = new URL(ogImage, siteOrigin).toString();
const publicApiBaseUrl = (import.meta.env.PUBLIC_API_BASE_URL || 'https://n2hhxvw3.ap-southeast.insforge.app').trim().replace(/\/$/, '');
const pageTitle = title === 'Home'
  ? 'TechMigos | Web, Mobile, AI & Backend Development'
  : `${title} | TechMigos`;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{pageTitle}</title>
    <meta name="title" content={pageTitle} />
    <meta name="description" content={description} />
    {noIndex && <meta name="robots" content="noindex, nofollow" />}

    <!-- Canonical -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={absoluteOgImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={pageTitle} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={absoluteOgImage} />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/icon.png" />
    <link rel="apple-touch-icon" href="/icon.png" />

    <!-- Theme Color -->
    <meta name="theme-color" content="#2563EB" />

    <meta name="techmigos-api-base-url" content={publicApiBaseUrl} />

    <!-- JSON-LD Structured Data -->
    <script is:inline type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "TechMigos",
      "url": siteOrigin,
      "logo": `${siteOrigin}/icon.png`,
      "description": description,
      "sameAs": [
        "https://linkedin.com/company/techmigos",
        "https://github.com/techmigos",
        "https://twitter.com/techmigos",
        "https://www.instagram.com/techmigos_official/"
      ],
      "contactPoint": {
        "@type": "ContactPoint",
        "email": "info@techmigos.com",
        "contactType": "customer service"
      }
    })} />
    <script is:inline>
      const apiBaseMeta = document.querySelector('meta[name="techmigos-api-base-url"]');
      const publicApiBaseUrl = apiBaseMeta ? apiBaseMeta.getAttribute('content') || 'https://n2hhxvw3.ap-southeast.insforge.app' : 'https://n2hhxvw3.ap-southeast.insforge.app';

      window.TechMigosConfig = {
        apiBaseUrl: publicApiBaseUrl,
      };

      window.techmigosApiUrl = function techmigosApiUrl(path) {
        const normalizedPath = String(path || '').startsWith('/') ? String(path || '') : `/${path || ''}`;
        const base = String(window.TechMigosConfig?.apiBaseUrl || '').replace(/\/$/, '');
        if (base.includes('.insforge.app')) {
          return `${base}/functions/techmigos-api`;
        }
        return base ? `${base}${normalizedPath}` : normalizedPath;
      };

      window.techmigosApiFetch = function techmigosApiFetch(path, options) {
        const normalizedPath = String(path || '').startsWith('/') ? String(path || '') : `/${path || ''}`;
        const headers = new Headers((options || {}).headers || {});
        const base = String(window.TechMigosConfig?.apiBaseUrl || '').replace(/\/$/, '');

        if (base.includes('.insforge.app')) {
          headers.set('x-techmigos-route', normalizedPath);
        }

        return fetch(window.techmigosApiUrl(path), {
          ...(options || {}),
          credentials: base.includes('.insforge.app') ? 'omit' : 'include',
          headers,
        });
      };

      window.techmigosGetCsrfToken = async function techmigosGetCsrfToken(message) {
        const response = await window.techmigosApiFetch('/api/csrf', { method: 'GET' });
        const result = await response.json().catch(() => null);
        if (!response.ok || !result?.token) {
          throw new Error(message || 'Secure form submission is unavailable. Please email info@techmigos.com directly.');
        }
        return String(result.token);
      };
    </script>
  </head>
  <body class="bg-white text-gray-900">
    <!-- Skip to main content link for accessibility -->
    <a href="#main-content" class="skip-link">Skip to main content</a>

    <!-- Custom Cursor -->
    <div id="cursor-dot" style="left:-100px;top:-100px;"></div>
    <div id="cursor-ring" style="left:-100px;top:-100px;"></div>

    <!-- Scroll Progress Bar -->
    <div id="page-progress" style="width:0%"></div>

    {!hideNavbar && <Navbar />}

    <main id="main-content">
      <slot />
    </main>

    <Footer />

    <script>
      // ============================================
      // CUSTOM CURSOR
      // ============================================
      const dot = document.getElementById('cursor-dot');
      const ring = document.getElementById('cursor-ring');
      const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
      const finePointer = window.matchMedia('(hover: hover) and (pointer: fine)');
      const allowEnhancedMotion = finePointer.matches && !reducedMotion.matches;
      let dotX = -100, dotY = -100;
      let ringX = -100, ringY = -100;

      if (!allowEnhancedMotion) {
        dot?.remove();
        ring?.remove();
      }

      if (allowEnhancedMotion && dot && ring) {
        const cursorDot = dot;
        const cursorRing = ring;

        document.addEventListener('mousemove', (e) => {
          dotX = e.clientX;
          dotY = e.clientY;
          cursorDot.style.left = dotX + 'px';
          cursorDot.style.top = dotY + 'px';
        }, { passive: true });

        function animateCursor() {
          ringX += (dotX - ringX) * 0.12;
          ringY += (dotY - ringY) * 0.12;
          cursorRing.style.left = ringX + 'px';
          cursorRing.style.top = ringY + 'px';
          requestAnimationFrame(animateCursor);
        }

        animateCursor();

        const interactiveEls = document.querySelectorAll('a, button, [role="button"], input, textarea, select, label');
        interactiveEls.forEach(el => {
          el.addEventListener('mouseenter', () => document.body.classList.add('cursor-hovering'));
          el.addEventListener('mouseleave', () => document.body.classList.remove('cursor-hovering'));
        });

        document.addEventListener('mouseleave', () => {
          cursorDot.style.left = '-100px';
          cursorDot.style.top = '-100px';
          cursorRing.style.left = '-100px';
          cursorRing.style.top = '-100px';
        });
      }

      // ============================================
      // SCROLL PROGRESS BAR
      // ============================================
      const progressBar = document.getElementById('page-progress');
      if (!reducedMotion.matches && progressBar) {
        window.addEventListener('scroll', () => {
          const scrollTop = window.scrollY;
          const docHeight = document.documentElement.scrollHeight - window.innerHeight;
          const progress = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
          progressBar.style.width = progress + '%';
        }, { passive: true });
      } else {
        progressBar?.remove();
      }

      // ============================================
      // SCROLL REVEAL (IntersectionObserver)
      // ============================================
      const revealObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
            revealObserver.unobserve(entry.target);
          }
        });
      }, { threshold: 0.1, rootMargin: '0px 0px -60px 0px' });

      document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale').forEach(el => {
        revealObserver.observe(el);
      });

      // ============================================
      // MAGNETIC BUTTON EFFECT
      // ============================================
      if (allowEnhancedMotion) {
        document.querySelectorAll('.btn-primary, .btn-ghost').forEach(btn => {
          btn.addEventListener('mousemove', (e: Event) => {
            const mouseEvent = e as MouseEvent;
            const rect = (btn as HTMLElement).getBoundingClientRect();
            const x = mouseEvent.clientX - rect.left - rect.width / 2;
            const y = mouseEvent.clientY - rect.top - rect.height / 2;
            (btn as HTMLElement).style.transform = `translate(${x * 0.1}px, ${y * 0.1}px) translateY(-2px)`;
          });
          btn.addEventListener('mouseleave', () => {
            (btn as HTMLElement).style.transform = '';
          });
        });
      }

      // ============================================
      // NUMBER COUNTER ANIMATION
      // ============================================
      const counterObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const el = entry.target as HTMLElement;
            const target = el.dataset.counter || '';
            const numericPart = parseInt(target.replace(/\D/g, ''));
            const suffix = target.replace(/[\d]/g, '');
            let current = 0;
            const duration = 1800;
            const step = numericPart / (duration / 16);
            const timer = setInterval(() => {
              current = Math.min(current + step, numericPart);
              el.textContent = Math.floor(current) + suffix;
              if (current >= numericPart) {
                el.textContent = target;
                clearInterval(timer);
              }
            }, 16);
            counterObserver.unobserve(el);
          }
        });
      }, { threshold: 0.5 });

      document.querySelectorAll('[data-counter]').forEach(el => {
        counterObserver.observe(el);
      });

      // ============================================
      // TILT CARD EFFECT
      // ============================================
      if (allowEnhancedMotion) {
        document.querySelectorAll('.tilt-card').forEach(card => {
          card.addEventListener('mousemove', (e: Event) => {
            const mouseEvent = e as MouseEvent;
            const rect = (card as HTMLElement).getBoundingClientRect();
            const x = (mouseEvent.clientX - rect.left) / rect.width - 0.5;
            const y = (mouseEvent.clientY - rect.top) / rect.height - 0.5;
            (card as HTMLElement).style.transform = `perspective(600px) rotateY(${x * 8}deg) rotateX(${-y * 8}deg) scale(1.01)`;
          });
          card.addEventListener('mouseleave', () => {
            (card as HTMLElement).style.transform = 'perspective(600px) rotateY(0) rotateX(0) scale(1)';
          });
        });
      }
    </script>
  </body>
</html>
