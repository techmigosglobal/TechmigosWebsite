---
import CanonicalIcon from '../../../components/crm/Icon.astro';

const { name, size = 18, stroke = 1.8, class: className = '' } = Astro.props;
---
<CanonicalIcon name={name} size={size} stroke={stroke} class={className} />
